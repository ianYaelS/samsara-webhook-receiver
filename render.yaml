# --------------------------------------------------------------------------
# render.yaml (Corregido v2)
#
# 1. CORREGIDO: 'databases' es una sección separada, no un 'type: psql'.
# 2. CORREGIDO: El 'startCommand' de webhook-listener ahora apunta a
#    'webhook_receiver:app' para coincidir con el nombre de tu archivo.
# --------------------------------------------------------------------------

databases:
  # 1. La Base de Datos PostgreSQL
  - name: postgres-db
    plan: free # Plan gratuito de Render. Nota: se suspenderá después de 90 días de inactividad.

services:
  # 2. El Listener de Webhooks (FastAPI)
  - type: web
    name: webhook-listener
    plan: free
    env: docker # Usar nuestro Dockerfile
    autoSuspend: false # CRÍTICO: Mantener vivo para recibir webhooks
    healthCheckPath: / # Comprobar que el servicio está vivo
    envVars:
      - key: DATABASE_URL
        fromService:
          type: psql
          name: postgres-db
          property: connectionString # Hereda la URL de la BD
      - key: PYTHON_VERSION
        value: "3.10.13"
    # --- COMANDO CORREGIDO para coincidir con 'webhook_receiver.py' ---
    startCommand: uvicorn webhook_receiver:app --host 0.0.0.0 --port $PORT

  # 3. El Dashboard (Streamlit)
  - type: web
    name: dashboard
    plan: free
    env: docker # Usar el MISMO Dockerfile
    autoSuspend: false # Mantener vivo para acceso
    healthCheckPath: /healthz # Endpoint de salud de Streamlit
    envVars:
      - key: DATABASE_URL
        fromService:
          type: psql
          name: postgres-db
          property: connectionString # Hereda la URL de la BD
      - key: SAMSARA_API_KEY
        sync: false # Poner este valor manualmente en el dashboard de Render
      - key: PYTHON_VERSION
        value: "3.10.13"
    # Comando para iniciar ESTE servicio
    startCommand: streamlit run streamlit_app.py --server.port $PORT --server.enableCORS false --server.enableXsrfProtection false
